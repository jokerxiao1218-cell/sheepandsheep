# 羊了个羊(Pygame 复刻)设计文档

> 创建时间:2026-09-08 | 状态:开发中(batch 1)

在 Linux 上用 Python + Pygame 复刻 2022 年爆火的微信小程序游戏《羊了个羊》:
多层堆叠牌堆 + 底部 7 槽位三消,两关制(第一关新手引导、第二关地狱难度),三道具,
鼠标点击玩。零外部素材(牌面运行时程序自绘),零网络。

玩法一句话:点开局的牌堆里没被压住的牌,牌进底部 7 格槽,凑满 3 张同图案立即消除,
全部消完过关;槽被 7 张凑不成对的牌塞满就失败。

---

## 1. 需求卡:羊了个羊 Linux 复刻

- 背景:用户想在自己 Linux 机器上玩到当年爆火的《羊了个羊》,顺便作为第二个
  Pygame 游戏项目(第一个是已交付的魔塔50层 `~/mota50`,187 测试)。
- 目标:`./run.sh` 弹出窗口,鼠标点牌,完整体验两关+三道具,输了能重来。
- 输入/输出:
  - 输入:鼠标点击牌面/按钮;键盘 Esc 退菜单、R 重开(辅助)。
  - 输出:窗口画面(牌堆、槽位、道具按钮、剩余计数)+ 每步状态变化 + 胜负结算画面。
  - 正常路径例子:开局第一层约 24 张明牌,点 3 张同图案 → 槽里 3 张瞬间消除、槽归零。
  - 边界例子:槽里 6 张(两种图案各 3 张正在凑)再点 1 张新图案 → 第 7 张入槽无三消 → 立即判负;
    B 区盲盒摞只有最顶上 1 张能点,拿走后露出下一张。
  - 异常路径例子:点击被压住的暗牌 → 无反应(core 返回拒绝,不崩不静默);
    道具用完再按 → 按钮已置灰,core 返回 False;游戏已结束后继续点牌操作 → core
    抛 ValueError(带说明),UI 层保证不触达。
- 约束:
  - 依赖:只用 Python 3.10 + pygame 2.6.1 + pytest 9.1.1(项目 `.venv` 已装好,
    与 mota50 同版本),**不引入任何新依赖**。
  - 错误处理:程序性错误(非法 id、状态机外操作)直接 raise 带上下文;
    交互性拒绝(点暗牌)返回结构化结果,UI 转为"没反应"。
  - 改动范围:全部在 `~/sheepandsheep/` 新目录内,零外部文件依赖
    (字体用系统字体,不打包 ttf——见 §10 风险)。
  - 性能:264 张牌规模,点击后全量重算遮挡 ≤ 10ms(O(n²)≈7 万次整数比较,无压力);
    主循环 60 FPS。
- 验收标准:
  - 测试:`./test.sh` 全绿(预计 100+ 用例,覆盖见 §6);
    无头自动对局:第一关随机瞎点必胜(鸽巢原理钉死)、第二关可解模式生成后按解回放必胜。
  - 手动:真机验证清单 §9 全部勾选。

用户已确认的三个决策(2026-09-08):Pygame 桌面窗口 / 完整版(两关+三道具)/
每 batch 验证通过自动 commit;仓库 `git@github.com:jokerxiao1218-cell/sheepandsheep.git`。

---

## 2. 现场调查

- 最相似的既有实现:**魔塔50层**(`~/mota50`,✅ 已交付,187 测试 + 全塔无头走查)。
  四件套惯例全部照搬:
  - `game/core/` 纯逻辑零 pygame,可独立单测(`mota50/game/core/state.py` 的风格:
    中文 docstring 讲清"干什么+设计文档依据"、数值不对直接 `raise ValueError` 带上下文、
    不静默吞错);
  - `game/assets.py` 运行时自绘素材(字符画 + 程序画矩形拼图,零图片文件);
  - `run.sh` / `test.sh`:自带 `.venv`(`--without-pip` + `pip --target` 装 pygame 2.6.1
    + pytest 9.1.1,已在 `~/sheepandsheep/.venv` 照此建好)、`unset PYTHONPATH`
    屏蔽系统 ROS 污染;
  - `scripts/walkthrough.py` 无头走查(`SDL_VIDEODRIVER=dummy`)做自动化验收。
- 新代码落在哪一层:本项目没有硬件抽象,分层为 **关卡数据 → 纯逻辑核心(core)→
  自绘素材(assets)→ 渲染交互(ui)**,与 mota50 的 data→core→assets/ui 完全同构。
- 相关文件清单(全新创建):

```
~/sheepandsheep/
  game/
    __init__.py
    data/levels.json      两关参数(照原版地图 JSON 字段命名)
    core/
      __init__.py
      tiles.py            牌数据结构 + 骨架生成 + 图案分配(两种模式)
      cover.py            遮挡判定(可点表 + 明/暗/隐藏三态)
      slot.py             7 槽位(聚集插入、三消、满判定)
      props.py            三道具 + 撤销历史
      game.py             Game 类——对外唯一门面(API 见 §5)
      solver.py           回溯求解器(工具/验收用,非游戏必需)
      tween.py            补间数值器(lerp + easing 纯函数)
    assets.py             自绘牌面/按钮 + 中文字体链
    ui.py                 场景栈(菜单/游戏/结算)+ Sprite 组 + 点击转发
    main.py              入口
  scripts/
    walkthrough.py        无头自动对局(验收)
    solve.py              对给定 seed 求解/验证可解性(工具)
  tests/                  pytest 单测
  run.sh / test.sh        一键运行 / 一键测试
  羊了个羊-设计文档.md      本文档
```

---

## 3. 业界调研

四线并行调研(机制 / 开源复刻 / pygame 工程 / 生成算法),274 次检索抓取,
关键结论与来源如下。**原版反编译仓库 + 社区抓包存档双确证的以 [G] 标注**;
单一来源的以 [1?] 标注,采用时在 §4 注明取舍。

### 3.1 机制 ground truth(照此 1:1 复刻)

| 机制 | 结论 | 依据 |
|---|---|---|
| 牌堆数据模型 | 地图 = 骨架(层/行/列固定)+ 图案分布(种子随机)。每张牌字段 `layerNum`(1=最底层,越大越靠上)、`rolNum`/`rowNum`(格坐标)、`moldType`(1=A区网格堆叠,2=B区盲盒摞)。每张牌占 2×2 逻辑格 | [G] 抓包 JSON 结构(tide-sec 抓包原文、NB-Dragon/SheepSolver 存档) |
| 图案分配 | `blockTypeData:{"图案id": 组数}`,每种图案张数恒为**组数×3**;全部展开成数组做 Fisher-Yates 洗牌后按骨架顺序填入 | [G] 原版反编译(P1kaj1uu/Wechat-Sheep-And-Sheep,发牌循环 `3*t[e]` 后 shuffle)+ 抓包数据 264=88组×3 精确吻合 |
| "非3倍数死牌" | **原版不存在**。每种牌恒为 3 的倍数;死局来自真随机布局本身,不是数量坑(xlegex/DI-sheep 的 trap 是社区再创作) | [G] SheepSolver 两份抓包地图(88组=264张、85组=255张,全整除)+ 反编译发牌逻辑 |
| 槽位 | 固定 7 格;进槽**插到槽内最后一张同图案牌后面**(无同类则插槽尾,同类永远挨在一起);连续 3 张同图案**进槽瞬间**消除并释放 | [G] 反编译 `addNewBlockNode`/`isCrushBlock`;warma13 逆向文档同 |
| 槽满判负 | 第 7 张入槽且未触发任何三消 → 立即判负;槽满后禁止再点 | [G] 反编译 `getCrushBlockNum`;DI-sheep/yulegeyu 同口径 |
| 遮挡(A区) | 任意与更高层牌发生 AABB 矩形重叠(哪怕压住一小角)→ 不可点。重叠公式:`A.rol < B.rol+2 且 B.rol < A.rol+2`(行同理);每步后全量重算 | [G] 抓包坐标模型 + warma13 文档公式;DI-sheep 同 |
| 遮挡视觉三态 | 明牌=无覆盖,正常;暗牌=被压,灰显不可点;隐藏牌=被 ≥2 个不同层覆盖,不渲染 | [1?] warma13 建模(覆盖层数去重计数),官方源码未泄露;DI-sheep 用"核心区被完全盖住"等价简化。本项目按"被 ≥2 个不同层压住=不渲染"实现(见 §4) |
| B区盲盒 | 2 摞、每摞 11 张、共 22 张;同摞 rol/row 相同、层号 1→11;每层向下偏移 6px 只露阴影条;只有摞顶可点 | [1?] warma13 单源 + gamersky 攻略"盲盒备用牌"旁证;参数作设计值采用 |
| 道具次数 | 每局各 1 次(移出/洗牌/撤销) | 3dm 攻略多处一致;反编译 cancelMask"每关只能使用一次"印证撤销 |
| 移出 | 从**槽头**顺序取前 3 张(玩家不可选)移到移出区;移出区牌可再点回槽;**使用移出会清空撤销历史**;**移出区有牌不算赢** | [G] 反编译 `moveOutBlock`/`checkGameWin` |
| 撤销 | 撤回**最后一张进槽**的牌回其来源(场上原位/移出区);跨消除不可撤(消除时清空撤销历史) | [G] 反编译 `cancelOneStep`(comeParent 标记);yulegeyu/warma13 同口径清历史 |
| 洗牌 | **只重排场上未点击牌的图案,位置/槽/移出区/已消全不动**(带旋转动画) | [G] 反编译 `updateBlockArea`:收集 type → shuffle → 逐张赋回,坐标不变 |
| 胜负 | 场上+槽+移出区全部清空 = 过关;槽满无消 = 失败;失败可无限次重试 | [G] 反编译 + DI-sheep `cur_item_num==0` 同 |
| 复活 | 原版第一次失败可看广告复活(复活=自动用一次移出腾槽)+赠移出道具;单机版无广告 → **不做复活,失败直接重开**(等价原版"不,谢谢"后的重新挑战) | 3dm 攻略;取舍见 §4 |
| 第一关参数 | 3 种图案的教学关:7 槽下任意 7 张按鸽巢原理必有 3 张同图案 → 只要一直点就必胜 | [G] 抓包分析(CSDN)+ 3dm"新手关"口径;精确张数抓包样本存疑(疑被破解工具改写),数量本项目自定 |
| 第二关参数 | **真实抓包硬数据(levelKey 90011):15 种图案(13种×6组 + 2种×5组)= 88 组 = 264 张、24 层**;纺锤形(中间层最宽);开局第一层明牌约 24 张 | [G] SheepSolver 抓包存档(levelKey 90011/90031 两份地图交叉);CSDN 抓包分析 |

### 3.2 架构参考(开源复刻横评,star 数为 2026-09 快照)

| 仓库 | star | 许可 | 参考价值 |
|---|---|---|---|
| opendilab/DI-sheep | 519 | **Apache-2.0** | 逻辑/渲染分离最彻底:React 前端零游戏逻辑,点击发 HTTP 给后端 Python 引擎。README 明说 `sheep_env.py` 可整文件拿走魔改。**本项目主参考**:把 HTTP 换成函数调用、React 换成 pygame 即同构 |
| liyupi/yulegeyu | 1831 | 无 LICENSE | 道具六件套与难度参数化最全;**只抄思路不抄代码**(无许可证默认保留版权) |
| chenxch/xlegex | 257 | MIT | API 设计最干净:`Game(nodes/selectedNodes/removeList)` + 事件回调。本项目 Game 门面照此形状 |
| NB-Dragon/SheepSolver | 122 | **AGPL-3.0** | 真实抓包地图存档(本项目第二关参数来源)+ 回溯求解器思路;**有传染性,不抄代码,自写** |
| StreakingMan/solvable-sheep-game | 408 | GPL-3.0 | 洗牌语义对照(它连位置一起洗,与原版不符——反证我们选"只洗图案"是对的) |

关键架构结论:
1. **遮挡判定选 O(n²) AABB 全量重算**(DI-sheep 与 solvable-sheep-game 同款):每次
   点击后全量重算,不维护增量关系网。264 张牌 7 万次比较毫秒级;洗牌/移出后天然一致,
   不会出增量维护 bug。反面教材是 yulegeyu 的双向引用绑定(点击 O(1) 但摘牌/撤回要
   小心维护反向引用,其 `doRevert` 疑似漏了重绑)。
2. **牌堆数据结构选"平铺列表 + None 占位"**(DI-sheep):数组顺序即层叠顺序,摘牌原地
   置 None,索引稳定,天然适配渲染循环与求解器动作空间。
3. **测试学 DI-sheep**(六个仓库里唯一有测试的):pytest 随机 rollout——随机采样合法
   动作跑到终局,断言状态结构与胜负条件。
4. **pygame 无现成羊了个羊参考**(GitHub "sheep pygame" 17 个结果全无关;"羊了个羊
   python" 42 个结果里除 DI-sheep/SheepSolver 外全是抓包通关助手)——逻辑层从
   DI-sheep 的 Python 实现移植思路,渲染层自建。

### 3.3 pygame 工程实践(结论均经官方文档 + 本机 pygame 2.6.1 实测双核实)

- **无头测试全链路本机已实测可行**:`pygame.init()` 前设 `SDL_VIDEODRIVER=dummy` +
  `SDL_AUDIODRIVER=dummy` → `set_mode` 正常、`event.post` 注入的
  MOUSEBUTTONDOWN/UP 能被 `event.get()` 取回、blit 后 `get_at` 返回真实像素
  (dummy surface 是真实内存位图)——与 mota50 走查惯例完全兼容。注意:注入事件前
  必须先 `set_mode`(未设视频模式时事件队列行为不正常)。
- **事件泵**:主循环每帧一次 `event.get()` 即同时完成取输入与泵队列;走查脚本直接调
  单步函数时要自行泵一次,太久不泵系统会判程序锁死。
- **命中检测**:牌是自绘矩形,`Rect.collidepoint` 足够(纯整数比较);mask 像素级命中
  在矩形素材上等价且更慢,不用。"上层牌压住下层时点重叠区选上层"是纯几何问题,
  放 core 做成纯函数(取点击点命中的、层号最高的牌)。
- **场景组织**:轻量场景栈——每场景一个类,统一 `handle_events/update(dt)/draw` +
  `enter()/exit()`(Game Programming Patterns 的 State 模式 + 下推状态栈);
  菜单→游戏→结算,结算用 push/pop 覆盖层。牌/槽/特效用官方 `Sprite/Group`。
- **补间**:不引库,手写 `lerp + easing` 纯函数放 `core/tween.py`(零 pygame 可单测),
  `ui` 每帧用 dt 推进。防连点:点击后 150ms 动画锁(solvable-sheep-game 同款)。
- **中文字体(最大坑,已实测)**:pygame 默认字体渲染中文是破碎占位符(实测"羊"只有
  35% 像素);`SysFont` 名字匹配有坑——必须写 `'notosanscjksc'`(带 sc 后缀,不带
  后缀匹配不到,本机已验证),最可靠是路径直载
  `/usr/share/fonts/opentype/noto/NotoSansCJK-Regular.ttc`。启动时自检:渲染一个
  中文字断言宽度接近字号,防静默回退。

### 3.4 生成与可解性

- **原版没有可解性保证**:发牌就是数量约束 + Fisher-Yates 正向随机,无任何校验
  (反编译确证);官方靠道具 + 看广告复活兜底,这就是"通关率 0.01%"的地狱设计。
- **业界没有公开的"逆向生成"实现**:调研所有主流复刻全部正向随机 + 道具兜底;
  唯一的求解验证是 SheepSolver 的回溯求解器(事后验证,自述"无法保证高效运行")。
- **本项目方案:双模式**——
  - 原味模式:照原版,数量约束 + 真随机,**不保证有解**(这就是第二关的"原汁原味");
  - 可解模式:**逆向构造**——先随机模拟一遍"聪明玩家的拿牌顺序"(每步从当前可点牌
    中随机挑 1 张,槽内做聚集/三消模拟,给拿过的牌按消除组编号),再把 88 个消除组
    映射到 15 种图案。拿牌顺序天然就是一条通关路径,构造即证明,**无需回溯搜索**。
    (纯逻辑、可复现、可单测;此为自研思路,业界无先例可抄,见 §10 风险)
- 理论背景:广义三消的"能否通关"判定是 NP-hard(arXiv:1403.5830,2014)——所以
  求解器只做工具(限时限节点),不承诺任意局必解。

调研来源主要清单:
- 原版反编译:https://github.com/P1kaj1uu/Wechat-Sheep-And-Sheep
- 抓包与数据:https://www.cnblogs.com/tide-sec/p/16696563.html 、
  NB-Dragon/SheepSolver(example/online_data_*.json)、
  https://blog.csdn.net/weixin_72684372/article/details/134076149
- 复刻项目:https://github.com/opendilab/DI-sheep 、https://github.com/liyupi/yulegeyu 、
  https://github.com/chenxch/xlegex 、https://github.com/StreakingMan/solvable-sheep-game
- 工程参考:https://gameprogrammingpatterns.com/state.html 、
  https://gameprogrammingpatterns.com/game-loop.html 、
  https://www.pygame.org/docs/ref/event.html 、https://www.pygame.org/docs/ref/rect.html
- 复杂性:https://arxiv.org/abs/1403.5830

未确证项(调研 uncertainties 摘要,与本项目相关):
- 第二关"开局明牌约 24 张"来自攻略口径,无逐层张数一手数据 → 骨架曲线自定(§4);
- 暗牌/隐藏牌官方精确判定未泄露,两套社区建模并存 → 按层级去重计数模型实现;
- 原版洗牌是否包含 B 区盲盒的图案:反编译只看到遍历"棋盘牌堆区",B 区是否在内
  无法确证 → 设计决定 A+B 区场上牌一起洗(盲盒看不见图案,行为无感知差异);
- 官方第一关精确张数抓包样本疑被破解工具污染 → 第一关数量自定。

---

## 4. 设计方案

### 4.1 总体架构(与 mota50 同构,分层照 DI-sheep)

```
main.py ─→ ui.py(场景栈/Sprite/点击)─→ game/core/game.py(Game 门面)
                                         │
        assets.py(自绘牌面/按钮/字体)←───┘(ui 只读 Game 状态画图,转发点击)
                                         │
              core/tiles.py(牌+骨架+图案分配)
              core/cover.py(遮挡/三态)
              core/slot.py(7槽聚集三消)
              core/props.py(道具+撤销历史)
              core/solver.py(求解器,仅 scripts 用)
              core/tween.py(补间纯函数)
              data/levels.json(两关参数)
```

依赖方向单向:main→ui→Game→(tiles/cover/slot/props);ui→assets。
core 全部零 pygame,不 import 任何 pygame 符号——单测不需要初始化 SDL。

### 4.2 关键设计选择(每条配"为什么")

1. **数据模型照原版**(`rolNum`/`rowNum`/`layerNum`/`moldType` 命名与 2×2 格 AABB
   重叠公式原样保留)——为什么:这是抓包+反编译双确证的 ground truth,照抄即 1:1,
   将来对拍社区数据也方便。
2. **牌表示:平铺 list + None 占位**(DI-sheep 模式,非 yulegeyu 的双向引用网)——
   为什么:遮挡全量重算在 264 张规模下毫无性能压力,却消灭了增量维护 bug 这一整类
   风险(yulegeyu 撤回疑似漏绑就是前车之鉴)。
3. **槽位"聚集插入 + 连续三消"**——为什么:这是原版反编译确证的行为(同类永远
   相邻),也是"槽满判负"语义正确的前提。
4. **双 seed**:`skeleton_seed`(骨架形态,关卡内固定——对应原版"每天一张图")+
   `pattern_seed`(图案分布,每局随机——对应原版 `map_seed`)——为什么:骨架与
   图案分离是原版架构,同时让单测 100% 可复现(两个 seed 都传死值)。
5. **可解模式用逆向构造而非回溯搜索**——为什么:构造即证明、毫秒级完成;回溯
   求解是 NP-hard,只能当工具不能当生成器。
6. **撤销历史:每次"牌进槽"压栈;消除时清空;移出道具使用时清空**(三处清空全部
   反编译/多复刻确证)——为什么:跨消除撤牌会拆掉已成组的三消,历史必须与
   "槽状态一致性"绑定。
7. **不做复活**——为什么:原版复活=看广告,单机版没有广告,失败直接重开体验等价。
8. **渲染窗口 540×960 竖屏**——为什么:原版是手机竖屏,牌堆在上 2/3、槽位在下
   1/3、B 区盲盒在牌堆下方两侧、移出区在槽上方,这个比例还原原版布局。
9. **牌面自绘:圆角矩形 + 单字符号**(如 玉米/草/萝卜/叉/剪刀 用一个汉字或几何
   符号画在色块上,15 种 = 15 个"底色+符号"组合)——为什么:零图片红线;汉字
   牌面在竖屏小尺寸下辨识度最高(原版牌面是农作物/工具图标);字体已实测可用。
10. **场景栈 + Sprite/Group**(State 模式)——为什么:菜单/游戏/结算三场景 +
    push/pop 覆盖层是本项目全部需求,自绘按钮 + collidepoint 已被 mota50 验证,
    不引 pygame-menu(违反零依赖约束)。

### 4.3 两关参数(定稿数值)

**第一关(新手关)**:
- 3 种图案,每种 4 组 = 12 组 = **36 张**(原版第一关为 3 种图案的教学关,精确张数
  无可靠数据,36 张是自定值:足够体验、5 分钟内能过);
- A 区 3 层小丘(层张数 10/16/10,levels.json 参数 edge=10/peak=16/total=36
  生成),无 B 区;
- 鸽巢必胜:3 种图案 7 槽,第 7 张入槽瞬间必有 3 张同图案 → 立即消除 → 槽永远
  填不满 → 一直点就必胜(单测用 100 局随机 rollout 钉死)。

**第二关(正式关,照抓包硬数据 levelKey 90011)**:
- 15 种图案:13 种 × 6 组 + 2 种 × 5 组 = 88 组 = **264 张**;
- A 区 22 层 + B 区 2 摞 × 11 张 = 22 张,合计 264;A 区纺锤形;
- 层曲线由参数生成(levels.json:edge=4、peak=18、peak_layer=11、total=242),
  涌现结果约 `[4,5,7,8,10,11,13,15,16,18,18,18,16,15,13,12,10,9,8,7,5,4]`
  ——两端 4 张、峰 18 张。原版"开局明牌约 24 张"是**涌现属性**(各层露出的
  表面牌合计),不是首层 24 张——§3.1 口径勘误,首层其实牌很少(edge=4);
- 层内布局:半格粒度随机错位、同层两两不重叠(原版网格摆放,同层互不压);
  skeleton_seed=20220924(原版上线日,好记)。

### 4.4 骨架生成器(tiles.py)

`make_skeleton(params, skeleton_seed) -> list[Tile]`(倒生长,2026-09-08 batch 1
实测修正——原"从底往上、上层必须压住下层"的方案在纺锤上升段(中间层比下层宽)
物理放不下,且原版 layerNum 本就是遮挡 z 序而非支撑高度,详见 §10 风险 4):
1. 从最顶层(牌最少)开始往底层铺:每张新牌以 STACK_BIAS=0.75 概率摞在已放好
   (更高层)牌的 ±1 邻域(必然重叠——堆叠感来源),其余自由铺(纺锤"下宽"
   就是这些自由牌扩张出来的);同层两两不重叠;界内;
2. 拒绝采样单张上限 200 次,放不下直接 LevelError 带层数/张数(不静默);
3. B 区:2 摞各 11 张,同摞同 (rol,row)、层号连排 1..11,`mold=2`;
4. 输出按 layer 升序的平铺列表(A 区在前 B 区在后;渲染顺序即列表顺序,
   后画的盖先画的,遮挡自然正确);
5. 同 skeleton_seed 必出同骨架(所有随机数走该 seed 的 `random.Random` 实例,
   不用全局 random——隔离性,测试与多实例互不干扰)。

**图案分配(两种模式)**:
- `deal_random(tiles, block_type_data, pattern_seed=None)`(原味):展开组数×3
  → Fisher-Yates 洗牌 → 依次填 type;pattern_seed=None 每局真随机;
- `deal_solvable(tiles, block_type_data, pattern_seed)`(可解):随机模拟拿牌顺序
  (每步随机挑一张当前可点牌,槽内聚集三消模拟,给牌标消除组号)→ 组号映射图案
  → 天然存在通关序列(该序列返回给 walkthrough 回放)。

### 4.5 撤销/移出的数据细节

- 撤销栈元素:`(tile_id, 来源)`,来源 ∈ {board, removed_out};
- 移出区:容量固定 3(首次使用移出道具后,槽头 3 张迁入);移出区牌**可点击**,
  点击即按普通进槽逻辑入槽并压撤销栈(来源=removed_out);撤销时牌退回移出区原位;
- 移出区已满时不能再用移出道具(返回 False;移出区只有腾空后……原版无第二次,
  每局限 1 次,此防御仅为防状态错乱);
- 胜利检查:场上空 ∧ 槽空 ∧ **移出区空**(反编译 `checkGameWin` 同)。

### 4.6 风险点与对策

| 风险 | 对策 |
|---|---|
| 264 张 24 层在 540×960 里摆不下/看不清 | 骨架生成器对包围盒做硬约束(所有牌落在 A 区格范围内,B 自动缩放;真机验证清单第一条人工确认) |
| 逆向构造生成的"可解局"难度可能偏低(拿牌顺序是瞎模拟的) | 可解模式明示"简单模式";原味模式才是还原;两模式分开验收 |
| 求解器在 264 张局上跑不动 | solver 限时限节点,超时如实报"未能证明可解",不假装;walkthrough 默认用第一关 + 第二关可解模式回放构造解 |
| 中文字体在无 CJK 机器上缺失 | 启动自检渲染中文字断言宽度,失败则明确报错指引装字体(不打包 ttf,见 §10) |
| 点击后 O(n²) 重算在低配机上掉帧 | 264 张实测毫秒级;如仍卡,再上缓存(点击才失效),预留接口不动契约 |

---

## 5. 模块划分与接口契约

规模:大(跨模块新项目)。以下契约为定稿,子代理/后续会话照此实现;
**契约变更必须先改本文档并告知用户**(dev-feature 纪律)。

### 5.1 模块清单与地盘

| 模块 | 职责一句话 | 只许动哪些文件 |
|---|---|---|
| M1 tiles | 牌数据结构、关卡参数解析、骨架生成、图案分配 | `game/core/tiles.py`、`game/data/levels.json`、`tests/test_tiles.py` |
| M2 cover | 遮挡判定:可点表、明/暗/隐藏三态、点击点命中 | `game/core/cover.py`、`tests/test_cover.py` |
| M3 slot | 7 槽位:聚集插入、三消、满判定 | `game/core/slot.py`、`tests/test_slot.py` |
| M4 props | 三道具与撤销历史 | `game/core/props.py`、`tests/test_props.py` |
| M5 game | Game 门面:状态机、对外唯一 API | `game/core/game.py`、`tests/test_game.py` |
| M6 solver | 回溯求解器 + 逆向构造解序列 | `game/core/solver.py`、`tests/test_solver.py`、`scripts/solve.py` |
| M7 assets | 自绘牌面/按钮/背景、中文字体链 | `game/assets.py`、`tests/test_assets.py` |
| M8 ui | 场景栈、布局、交互、动画、入口 | `game/ui.py`、`game/main.py`、`tests/test_ui.py`、`scripts/walkthrough.py`、`run.sh`、`test.sh` |
| M9 tween | 补间纯函数 | `game/core/tween.py`、`tests/test_tween.py` |

文件互不重叠。依赖方向(无环):
`main→ui→Game→{cover,slot,props,tiles}`;`ui→{assets,tween}`;`solver→{cover,slot}`。

### 5.2 接口契约

**数据结构(全体共享,定义在 tiles.py)**:

`Tile` 用 `@dataclass` 实现(字段与下表一致,`Tile.as_dict()` 转 dict;契约说的是
字段集与语义,Python 表示形式为实现细节):

**M1 tiles 对外**:
- `load_levels() -> dict`:读 `data/levels.json`,结构错 raise ValueError 带字段名;
- `make_skeleton(params: dict, skeleton_seed: int) -> list[Tile]`:只填位置/层/mold,
  type=0;输出按 layer 升序;骨架内所有随机数走 `random.Random(skeleton_seed)`;
- `deal_random(tiles, block_type_data, pattern_seed=None) -> None`:原地填 type
  (Fisher-Yates;seed=None 用 `random.Random()` 每局随机);
  deal_solvable 原属 M1,实施时挪入 M6(逆向构造要复用 cover/slot 的判定逻辑,
  依赖方向 tiles→cover 不如 solver→{cover,slot} 干净)——契约同步此变更:
- (M6 solver)`deal_solvable(tiles, block_type_data, pattern_seed: int) -> list[str]`:
  原地填 type 并返回一条通关点击序列(tile id 列表)。

**M2 cover 对外**:
- `covered(a: Tile, b: Tile) -> bool`:AABB 重叠(格公式,2×2);
- `refresh_cover(tiles: list[Tile]) -> None`:全量重算——为每张 zone=="board" 牌
  置 `clickable`(无任何更高层重叠)与 `visible`("bright"/"dim"/"hidden":
  被压者为 dim,被 ≥2 个不同 layer 压者为 hidden);
- `pick(tiles, pos_px, to_px) -> Tile | None`:点击点命中的、layer 最高的场上牌
  (几何换算由调用方传 px→格 的映射函数)。
  ——注:`pick` 的像素换算让 ui 做成纯函数传入,cover 只收逻辑格。

**M3 slot 对外**:
- `insert(slot: list[Tile], tile: Tile) -> list[Tile]`:聚集插入(同类尾后插),
  返回新列表;触发三消时返回已消 3 张;
- `is_full(slot) -> bool`:len==7(三消同步完成后才判,动画期间不占位);
- `would_lose(slot, tile) -> bool`:预判(留防御用)。

**M4 props 对外**:
- `move_out(slot, out_zone, history) -> list[Tile]`:槽头 3 张 → out;**清空
  history**;槽不足 3 张返回原样+False(不部分移动);
- `shuffle_tiles(tiles, rng) -> None`:场上(zone=="board")牌的 type 重新洗牌
  位置不动;槽/移出区/已消不动;
- `undo_pop(history) -> (tile_id, source) | None`:弹栈顶。

**M5 Game 门面(ui 只认它)**:
```python
class Game:
    def __init__(self, level_id: int, mode: str = "classic",
                 pattern_seed: int | None = None): ...
        # mode: "classic" 原味 / "solvable" 可解;seed 传死值=复现局
    def click(self, tile_id: str) -> ClickResult: ...
        # ClickResult = {"ok": bool, "reason": str, "eliminated": list[Tile]}
        # ok=False 仅因:牌被压/不在可点区/移出区牌点击合法照常 ok
    def use_prop(self, kind: str) -> bool: ...
        # kind ∈ {"move_out", "shuffle", "undo"};不可用返回 False
    @property
    def status(self) -> str: ...        # "playing"/"win"/"lose"
    @property
    def tiles(self) -> list[Tile]: ...  # 全量牌(ui 渲染只读)
    def snapshot(self) -> dict: ...      # 关键统计(场上/槽/移出区计数、道具余量)
```
错误处理契约:程序性错误(id 不存在、status≠playing 时 click、非法 kind)
→ raise ValueError 带完整上下文;交互拒绝(点暗牌)→ ClickResult.ok=False。

**M6 solver 对外**:
- `solve(game: Game, budget: SolveBudget) -> list[str] | None`:限时限节点的
  回溯求解,返回点击序列或 None(超时/证伪不明);不修改 game(克隆状态跑);
- `make_solver_game(...)`(walkthrough 用工厂)。

**M7 assets 对外**:
- `tile_face(type_id: int, size: int) -> pygame.Surface`:自绘牌面(圆角矩形+
  单字符号+底色),按 (type,size) 缓存;
- `cn_font(size: int) -> pygame.font.Font`:字体链(路径直载 NotoSansCJK →
  SysFont('notosanscjksc') 回退),启动自检断言中文宽度;
- `draw_button(...) / draw_slot_bg(...)` 等少量 ui 专用绘制。

**M8 ui 对外(main 用)**:
- `run() -> int`:整个游戏主循环(场景栈),返回退出码;
- 场景类:`MenuScene / PlayScene / ResultScene`,统一
  `handle_events(events) / update(dt) / draw(screen)` + `enter()/exit()`。

**M9 tween 对外**:
- `lerp(a, b, t)`、`ease_out_quad(t)` 等纯函数;
- `Tween(tween 管理器)`:add(duration, ease, on_update, on_done)、update(dt)。

### 5.3 模块级验收(并行/单独开发时各模块照此自验)

- M1:给定固定双 seed → 牌列表逐字段可复现;数量恒为 3 的倍数;A 区牌落界内;
  deal_solvable 返回的序列在 M5 完成后回放必胜(集成验收再验一次);
- M2:构造 3 层 5 牌手工算例钉死可点表与三态;B 区摞只有顶可点;
- M3:聚集插入次序表(手推 12 种情况);第 3 张瞬间消;7 张无三消判负;
- M4:移出/撤销/洗牌各效果的"字段级"断言;
- M5:随机 rollout(DI-sheep 式)状态结构;第一关瞎点必胜;结束后操作 raise;
- M6:小构造局(手工摆 12 张必有解)能解出;无解构造局返回 None;
- M7:dummy 下渲染 15 种牌面 get_at 断言底色像素;中文宽度自检;
- M8:dummy 下 event.post 注入点击,walkthrough 全流程跑通;
- M9:给定 t 断言插值与 easing 值。

---

## 6. 测试计划

跑法:`cd ~/sheepandsheep && ./test.sh`(= `unset PYTHONPATH;
.venv/bin/python -m pytest tests/ -v`)。数字具体、全部可断言。

### 6.1 正常路径
1. 骨架生成:skeleton_seed=20220924 → 第二关 264 张、22+2 摞、首层 4 张
   (明牌数是涌现属性,集成验收人工目测 20~40 张),两次生成逐字段一致;
2. 图案分配(classic 固定 seed):15 种图案张数 = [6]*13+[5]*2 组×3;
   两次同 seed 一致,不同 seed 不同;
3. 点一张明牌 → zone 变 slot、槽=1;点 3 张同图案(直点)→ 消除、槽归 0、
   ClickResult.eliminated 长 3;
4. 聚集插入:槽 [A,A,B] 进 A → [A,A,A,B]→三消 [B];槽 [A,B] 进 A → [B,A,A,A]
   →消 [B];(手推真值表全列);
5. 遮挡解除:点走上层牌后,下层牌 clickable 由 False 变 True(refresh 全量);
6. B 区:摞顶可点、拿走后下一张变摞顶;B 区牌不与 A 区互压;
7. 胜利:全消后 status=="win";失败:凑满 7 张无三消 status=="lose"。

### 6.2 边界
8. 空槽点牌/槽 6 张凑 2 对再点新图案(第 7 张入槽)→ 立即 lose;
9. 槽 6 张点第 7 张恰好三消 → 不输,继续;
10. 移出区 3 张回槽后胜局判定:场上+槽空但移出区有牌 → **不算赢**(继续玩);
11. 撤销栈空时用 undo → False;道具每种用第 2 次 → False;
12. deal_solvable 返回的序列整体回放 → status=="win"(必胜钉死);
13. 洗牌后:场上牌 type 集合不变(多重集合相等)、位置逐张不变、槽/移出区不动。

### 6.3 异常
14. click 不存在 id → ValueError 带该 id;status=="lose" 后 click → ValueError;
15. use_prop 传非法 kind → ValueError;deal 前数量校验:组数和×3 ≠ 牌数 →
    ValueError(不静默);
16. B 区构造:同摞非顶层牌 clickable==False(哪怕没被 A 区压)。

### 6.4 随机 rollout(DI-sheep 式结构测试)
17. 第一关 100 局随机瞎点(每次从 clickable 随机挑)→ 100% win(鸽巢钉死);
18. 第二关 classic 固定 seed 局,随机 rollout 到终局 → 终态 ∈ {win, lose},
    且 lose 时槽恰好 7 张无三消(不变量);
19. 每步后不变量断言:clickable 集合与手工重算一致、槽内无连续三同(三消即时性)。

### 6.5 渲染/交互(dummy 无头)
20. set_mode(540,960) 成功;15 种牌面 blit 后 get_at 中心点 = 各自底色;
21. post 注入 MOUSEBUTTONDOWN/UP(带 pos)→ ui 转发给 Game → 槽+1(链路通);
22. 中文字体自检:渲染"羊"宽度 ≥ size×0.8(防静默回退);
23. walkthrough:第一关随机玩到 win 退出码 0;第二关 solvable 按构造序列回放 win
    退出码 0。

### 6.6 补间
24. lerp(0,10,0.5)==5;ease_out_quad(0)/1 == 0/1;Tween 到期触发 on_done。

---

## 7. Batch 划分与进度

原则:每个 batch 完成时 `./test.sh` 全绿(可编译可运行);按依赖排;
单 batch diff 控制在一屏可读。

| # | 目标(做完后系统什么状态) | 涉及文件 | 验证方式 | 并行组 | 状态 |
|---|---|---|---|---|---|
| 1 | 项目骨架+数据模型+生成器:牌结构、levels.json、骨架生成、经典图案分配,单测绿 | core/tiles.py, data/levels.json, tests/test_tiles.py, test.sh | §6.1-1/2、§6.3-15 | A | ✅ |
| 2 | 核心玩法:遮挡三态+7槽聚集三消+Game 门面(点牌/胜负),单测绿 | core/cover.py, slot.py, game.py, tests/test_cover.py, test_slot.py, test_game.py | §6.1-3~7、§6.2-8/9、§6.3-14/16、§6.4-17 | A | 待执行 |
| 3 | 道具系统:移出/洗牌/撤销+历史清空,单测绿 | core/props.py, game.py(接道具), tests/test_props.py, test_game.py 补 | §6.2-10/11/13 | A | 待执行 |
| 4 | 求解器+无头自动对局:deal_solvable 逆向构造、solve.py 回溯求解、walkthrough.py 随机/回放对局 | core/solver.py, scripts/solve.py, scripts/walkthrough.py, tests/test_solver.py | §6.2-12、§6.4-18/19、§6.6-23 前半 | A | 待执行 |
| 5 | 渲染交互:自绘牌面+字体链、场景栈、点击转发、run.sh、tween 动画 | assets.py, core/tween.py, ui.py, main.py, tests/test_assets.py, test_ui.py, test_tween.py, run.sh | §6.5-20~22、§6.6-24、walkthrough 端到端 | B | 待执行 |
| 6 | 集成验收+收尾:全量测试、两关无头通关、README、真机清单交付 | README.md, 本文档 §8/§9 更新 | 全部 §6 + walkthrough 两关退出码 0 | — | 待执行 |

并行组说明:A 组 1-4 全是 core(文件不重叠,但 game.py 逐步长肉——按依赖串行
执行最稳,不派并行代理);B 组 5 渲染层。规模评估后**放弃多代理并行开发**(核心
逻辑高度耦合于 game.py 门面,并行收益小于集成风险;ultracode 的并行性改用在
每个 batch 完成后的对抗性 code review 子代理上——batch 交付→独立 reviewer
对照契约与测试计划挑毛病,这比并行写更快更稳)。
集成验收(batch 6)是独立 batch,验"接口两侧真的对得上":全量测试 + 两关端到端
无头通关 + git diff 对照测试计划无篡改。

## 8. 执行记录

| 日期 | Batch | 结果 | 备注(Commit/问题) |
|---|---|---|---|
| 2026-09-08 | — | 环境就绪 | 仓库 init、venv(pygame 2.6.1+pytest 9.1.1)、四线调研完成 |
| 2026-09-08 | 1 | ✅ 16 用例绿 | 首轮"上层必须压下层"约束在纺锤上升段物理不可行(16>10 放不下邻域),改倒生长生成器+堆叠密度断言;文档 §4.3/§4.4/§6.1/§10-4 同步修正 |

## 9. 真机验证清单(batch 全 ✅ 后交给用户执行)

前置:`cd ~/sheepandsheep && ./run.sh`(自动用 .venv,弹窗 540×960)。

- [ ] **首屏**:主菜单出现"羊了个羊"中文标题与两个关卡入口,中文清晰无方块
  (字体链自检通过的视觉印证);
- [ ] **第一关布局**:3 种图案牌面、3 层堆叠可辨、底部 7 槽空;
- [ ] **核心玩法**:点 3 张同图案 → 飞入槽(有动画)→ 三消清空;上层牌拿走后
  下层暗牌变亮可点;
- [ ] **异常路径 1**:点暗牌(被压灰显)→ 无反应、无报错、不崩;
- [ ] **槽满失败**:故意凑 7 张互不成对的牌 → 立即失败结算画面,"重新开始"
  回到当前关;
- [ ] **移出道具**:槽里有牌时点移出 → 前 3 张进移出区;移出区牌可点回槽;
  移出区有牌时全消不算赢(场上+槽空时继续玩,把移出区点完才结算 win);
- [ ] **撤销道具**:点错牌撤销 → 牌回到场上原位可再点;发生三消后撤销按钮
  不可用(历史已清);
- [ ] **洗牌道具**:场上图案重新分布、位置不动、槽内不动;已用过的道具按钮置灰;
- [ ] **第二关(原味)**:15 种图案、22 层纺锤+B 区 2 摞盲盒,摞顶可点、拿下后
  露下一张;整体体验"很难"属正常(原版 0.01% 通关率口径);
- [ ] **第二关(可解模式)**:菜单选可解模式开局,体验明显更顺(至少存在通关路径);
- [ ] **性能**:第二关 264 张全程 60 FPS 无卡顿(主观:点击响应即时);
- [ ] **退出重进**:Esc 回菜单、重开局 pattern 重新随机(牌面分布变化)。

## 10. 遗留风险与未验证点

1. **中文字体可移植性**:字体用系统字体(NotoSansCJK),不打包 .ttc——与"零
   外部素材"惯例一致,但拷到无 CJK 字体的裸机上会启动报错(自检会明确提示)。
   如需分发再决策打包(需要用户点头,属范围外);
2. **第一关参数是自定值**:原版第一关精确张数无可靠一手数据(抓包样本疑被污染);
3. **暗/隐藏牌官方判定未泄露**:本项目按"被 ≥2 个不同层压住=不渲染"的社区模型
   实现,若与原版有出入属已知取舍(§3.1 表);
4. **骨架生成器层号语义——已决(2026-09-08,batch 1 实测踩坑后修正)**:原版
   `layerNum` 是遮挡渲染的 z 序、不是物理支撑高度(抓包纺锤地图含跨层悬空);
   生成器由"从底往上长、上层必须压住下层"改为**倒生长**(顶层牌最少先铺 →
   底层每层变宽,STACK_BIAS=0.75 摞邻域+自由铺扩张)——原方案在纺锤上升段
   (中间层 16 张 > 底层 10 张)邻域物理放不下,首轮测试实测复现。堆叠密度
   ≥50% 改由测试钉死(防散成平铺);
5. **B 区是否参与原版洗牌无确证**:本项目决定 A+B 区场上牌一起洗(玩家无感知
   差异);
6. **solver 对 264 张局不保证出解**:NP-hard,限时限节点,超时如实报告;
   walkthrough 第二关用可解模式的构造序列回放,不依赖 solver;
7. **逆向构造可解局的实际难度未标定**:构造解存在 ≠ 容易找到,真机体验后可能
   需调拿牌模拟策略(如偏好拿当前可点中"槽内已有类型"),留到真机反馈。
